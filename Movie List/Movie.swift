import UIKit

struct Movie {
    let movieName: String
    let hasWatched: Bool
    
    init(movieName: String, hasWatched: Bool) {
       self.movieName = movieName
        self.hasWatched = hasWatched
    }
    
}
